name: hexlet-ci-app
on: push
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - name: setup project
      run: make setup
    - name: run lint
      run: make lint
    - name: run tests
      run: make test
